<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Partial Info.plist. GENERATE_INFOPLIST_FILE stays YES, so Xcode merges every
	  INFOPLIST_KEY_* build setting (display name, usage strings, orientations) on top of
	  this file. Only keys that setting cannot express belong here.

	  UIBackgroundModes is one of them: there is no working INFOPLIST_KEY_UIBackgroundModes,
	  and setting one is silently ignored rather than failing the build. Without the
	  bluetooth-central mode below, CBCentralManager throws
	  NSInternalInconsistencyException at launch the moment it is created with a
	  CBCentralManagerOptionRestoreIdentifierKey (see RingManager.init).
	-->
	<key>UIBackgroundModes</key>
	<array>
		<string>bluetooth-central</string>
	</array>
</dict>
</plist>
